export default interface IUser {
    username?: string;
    image?: string;
}

export interface IPlaceHolderUser {
    email: string,
    id: number,
    name: string,
    phone: string,
    username?: string,
    website: string,
}